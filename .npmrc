# Supply-chain hardening: installs must be deliberate and non-executing by default.
ignore-scripts=true
save-exact=true
audit=true
fund=false
package-lock=true

# npm 10 currently reports this as an unknown/undefined config locally, so do not
# enable it here until the project runner is on an npm version that supports it.
# Target once supported: min-release-age=60
